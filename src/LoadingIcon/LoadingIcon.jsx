import React from "react";

import styles from "./LoadingIcon.styles.scss";

const LoadingIcon = () => (
  <div className={styles.container}>
    <div className={styles.loader} />
  </div>
);

export default LoadingIcon;
